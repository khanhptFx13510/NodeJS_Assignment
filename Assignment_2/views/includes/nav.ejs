<nav class="navbar navbar-expand-lg navbar-light indigo-400">
   <div class="container">
      <a class="navbar-brand" href="<% isAuthenticated ? '/' : '/login' %>">
         <h2 class="title-color">Personal Information Tracking</h2>
      </a>
      <div>
         <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
         </button>
         <div class="collapse navbar-collapse" id="navbarText">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
               <% if (isAuthenticated) { %>
                  <li class="nav-item">
                     <a class="nav-link <%= path === '/' ? 'active' : '' %>" href="/">
                        Timekeeping
                     </a>
                  </li>
                  <li class="nav-item">
                     <a class="nav-link <%= path === '/profile' ? 'active' : '' %>" href="/profile">Profile</a>
                  </li>
                  <li class="nav-item">
                     <a class="nav-link <%= path === '/salary' ? 'active' : '' %>" href="/salary">Salary</a>
                  </li>
                  <% if(isManager){ %>
                     <li class="nav-item">
                        <a class="nav-link <%= path === '/conform' ? 'active' : '' %>" href="/conform">Manager Conform</a>
                     </li>
                  <% } %>
                  <li class="nav-item">
                     <a class="nav-link <%= path === '/covid-19' ? 'active' : '' %>" href="/covid-19">Covid-19</a>
                  </li>
               <% } %>            

               <% if (!isAuthenticated) { %>
               <li class="nav-item">
                  <a class="nav-link <%= path === '/login' ? 'active' : '' %>" href="/login">login</a>
               </li>
               <% } else { %>
               <li class="nav-item">
                  <form action="/logout" method="post">
                     <button type="submit" 
                     style="border: none; 
                        background-color: rgba(103 ,16, 242, -0.43);
                        margin-top: 6px;">
                        Logout
                     </button>
                  </form>
               </li>
               <% } %>
            </ul>
            <span class="navbar-text">
               
            </span>

         </div>
      </div>
   </div>
</nav>